(function (factory) {
    typeof define === 'function' && define.amd ? define(factory) :
    factory();
})((function () { 'use strict';

    function sum(num1, num2) {
        return num1 + num2;
    }

    console.log(sum(1, 2));

}));
//# sourceMappingURL=index.js.map
